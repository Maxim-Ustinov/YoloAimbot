"""Доменная модель тиммейта (Teammate) — союзник, по которому НЕ наводимся.

Friend/foe — это v2 (простой путь, т.к. игрок всегда за одну команду):
детектор получит 3-й класс `teammate` (скины команд различаются: CLA камуфляж
vs RVSF синие), а логика наводки берёт цели только из enemy и исключает
teammate'ов.

Пока НЕ задействовано детектором: текущие 300 кадров — FFA (тиммейтов нет),
для v2 нужен отдельный сбор в командном режиме (TDM).
"""

from dataclasses import dataclass

from .geometry import Box


@dataclass
class Teammate:
    """Обнаруженный союзник. Нужен только чтобы НЕ стрелять по нему, поэтому
    достаточно рамки тела — голова и точка прицеливания не нужны."""

    body: Box
    confidence: float = 0.0
